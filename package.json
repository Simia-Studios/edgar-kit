{
  "name": "edgar-kit",
  "version": "2.0.0",
  "private": false,
  "description": "Type-safe SDK for SEC EDGAR filings, submissions, search, archives, XBRL, financials, and company data.",
  "license": "ISC",
  "bin": {
    "edgar-kit": "./dist/cli.mjs"
  },
  "files": [
    "dist/**",
    "README.md"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    },
    "./cli": {
      "types": "./dist/cli.d.mts",
      "default": "./dist/cli.mjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsdown --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && tsdown",
    "build:clean": "tsc --noEmit && tsdown --clean",
    "release": "./publish.sh",
    "format": "oxfmt .",
    "verify:cli": "node scripts/verify-cli-help.mjs",
    "check": "oxfmt --check . && pnpm typecheck && pnpm test && pnpm build && pnpm verify:cli",
    "prepare": "lefthook install"
  },
  "dependencies": {
    "@effect/cli": "0.75.1",
    "@effect/platform-node": "0.106.0",
    "effect": "3.21.2"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "lefthook": "2.1.6",
    "oxfmt": "0.47.0",
    "tsdown": "0.21.10",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  },
  "packageManager": "pnpm@10.33.2"
}
